# Quick Start Guide

> คู่มือเริ่มต้นใช้งาน Gov Contract Platform อย่างรวดเร็ว

---

## 🎯 เป้าหมาย

คู่มือนี้จะพาคุณเริ่มต้นใช้งานระบบภายใน 10 นาที

---

## 📋 สารบัญ

1. [สิ่งที่ต้องเตรียม](#-สิ่งที่ต้องเตรียม)
2. [ขั้นตอนการติดตั้ง](#-ขั้นตอนการติดตั้ง)
3. [การเข้าใช้งานครั้งแรก](#-การเข้าใช้งานครั้งแรก)
4. [เริ่มต้นใช้งาน](#-เริ่มต้นใช้งาน)
5. [แก้ไขปัญหาเบื้องต้น](#-แก้ไขปัญหาเบื้องต้น)

---

## ✅ สิ่งที่ต้องเตรียม

### ความต้องการขั้นต่ำ

| รายการ | ขั้นต่ำ | แนะนำ |
|--------|---------|-------|
| Docker | 20.10+ | Latest |
| Docker Compose | 2.0+ | Latest |
| RAM | 8 GB | 16 GB |
| Disk Space | 20 GB | 50 GB |
| OS | Linux/Mac/Windows WSL2 | Linux |

### ตรวจสอบระบบ

```bash
# ตรวจสอบ Docker
docker --version
docker compose version

# ตรวจสอบพื้นที่ดิสก์
df -h

# ตรวจสอบ RAM
free -h
```

---

## 🚀 ขั้นตอนการติดตั้ง

### Step 1: Clone Repository

```bash
git clone https://github.com/n00n0i/gov-contract-platform.git
cd gov-contract-platform
```

### Step 2: ตั้งค่า Environment (ถ้าจำเป็น)

```bash
# Copy ไฟล์ตัวอย่าง
cp .env.example .env

# แก้ไขค่าต่างๆ (ถ้าต้องการ)
nano .env
```

### Step 3: รันระบบ

```bash
cd infra
docker compose up -d
```

### Step 4: รอให้ระบบพร้อม

```bash
# ตรวจสอบสถานะ
docker compose ps

# ดู Logs
watch docker compose ps
```

รอจนกว่าทุก Service จะขึ้นสถานะ `healthy` หรือ `running` (ประมาณ 1-2 นาที)

---

## 🔐 การเข้าใช้งานครั้งแรก

### 1. เปิดเบราว์เซอร์

```
http://localhost:3000
```

### 2. Login ด้วยบัญชีเริ่มต้น

| ฟิลด์ | ค่า |
|-------|-----|
| Username | `admin` |
| Password | `admin123` |

### 3. เปลี่ยนรหัสผ่าน (สำคัญ!)

```
1. คลิกที่ชื่อผู้ใช้ (มุมขวาบน)
2. เลือก "โปรไฟล์"
3. กรอกรหัสผ่านใหม่
4. กด "บันทึก"
```

---

## 📖 เริ่มต้นใช้งาน

### สร้างสัญญาใหม่

```
1. ไปที่เมนู "สัญญา"
2. กดปุ่ม "สร้างสัญญาใหม่"
3. กรอกข้อมูล:
   - เลขที่สัญญา
   - ชื่อสัญญา
   - ผู้รับจ้าง
   - มูลค่า
   - ระยะเวลา
4. กด "บันทึก"
```

### อัปโหลดเอกสาร

```
1. ไปที่ "อัปโหลด"
2. เลือกสัญญาที่ต้องการ
3. เลือกประเภทเอกสาร
4. ลากไฟล์มาวาง หรือคลิกเลือก
5. รอระบบ OCR ประมวลผล
6. ตรวจสอบข้อมูลที่ถอดความได้
7. กด "บันทึก"
```

### ดู Dashboard

```
หน้าแรก (/) จะแสดง:
- สถิติสัญญาทั้งหมด
- สัญญาใกล้หมดอายุ
- กิจกรรมล่าสุด
- งานที่ต้องทำ
```

---

## 🔧 แก้ไขปัญหาเบื้องต้น

### ปัญหา 1: ไม่สามารถเข้าเว็บได้

```bash
# ตรวจสอบว่า Container รันอยู่หรือไม่
docker compose ps

# ถ้าไม่รัน ให้ restart
docker compose restart frontend backend

# ตรวจสอบ Logs
docker compose logs frontend
docker compose logs backend
```

### ปัญหา 2: Login ไม่ได้

```bash
# ตรวจสอบว่า Backend ทำงานปกติ
curl http://localhost:8000/health

# ถ้าไม่ตอบสนอง ให้ restart
docker compose restart backend
```

### ปัญหา 3: อัปโหลดไฟล์ไม่ได้

```bash
# ตรวจสอบ MinIO
docker compose ps minio

# Restart MinIO ถ้าจำเป็น
docker compose restart minio
```

### ปัญหา 4: ระบบช้า

```bash
# ตรวจสอบการใช้ทรัพยากร
docker stats

# ถ้าใช้ RAM เยอะ ให้เพิ่ม Docker Memory Limit
# หรือ restart ระบบ
docker compose restart
```

---

## 📞 ติดต่อสนับสนุน

- **GitHub Issues**: https://github.com/n00n0i/gov-contract-platform/issues
- **Documentation**: ดู [WALKTHROUGH.md](./WALKTHROUGH.md)

---

## ✅ Checklist หลังติดตั้ง

- [ ] Login ได้สำเร็จ
- [ ] เปลี่ยนรหัสผ่านเริ่มต้นแล้ว
- [ ] สร้างสัญญาทดสอบได้
- [ ] อัปโหลดเอกสารได้
- [ ] Dashboard แสดงข้อมูลถูกต้อง
- [ ] ระบบแจ้งเตือนทำงาน

---

> 🎉 **ยินดีด้วย!** คุณพร้อมใช้งาน Gov Contract Platform แล้ว
